{
  "name": "ca-inter",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "predev": "node scripts/sync-pdf-worker.mjs",
    "dev": "astro dev --host 0.0.0.0 --port 4323",
    "prebuild": "node scripts/sync-pdf-worker.mjs",
    "build": "astro build",
    "postbuild": "node scripts/strip-dist-pdfs.mjs",
    "preview": "astro preview --host 0.0.0.0 --port 4324",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^4.16.18",
    "papaparse": "^5.4.1",
    "pdfjs-dist": "^4.7.76"
  },
  "devDependencies": {
    "@types/papaparse": "^5.3.15"
  }
}
